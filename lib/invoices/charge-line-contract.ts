/** Canonical analytical charge-line schema version (bump when contract changes). */
export const CANONICAL_CHARGE_LINE_VERSION = '1'

/** FedEx parser version — bump when column mapping or unpivot rules change. */
export const FEDEX_PARSE_VERSION = 'fedex-v2'

export const WWE_PARSE_VERSION = 'wwe-v1'

export const UPS_PARSE_VERSION = 'ups-csv-v1'
